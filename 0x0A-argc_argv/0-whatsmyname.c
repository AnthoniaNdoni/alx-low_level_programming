#include <stdoi.h>
#include "main.h"

/**
 * main - prints the the name of program
 * @argc: no of arguments
 * @argv: an array of arguments
 *
 * Return: 0
 */
int main(int argc -attribute -((unused)), char *argv[])
{
	printf("%s\n", * argv);

	return (0);
}
